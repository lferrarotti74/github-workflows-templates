name: Validate: Sync Main to Target

on:
  workflow_dispatch:
    inputs:
      source_branch:
        description: 'Branch to sync from (default: main)'
        required: false
        type: string
        default: 'main'
      target_branch:
        description: 'Branch to sync into (default: main)'
        required: false
        type: string
        default: 'main'

permissions:
  contents: write

jobs:
  validate:
    uses: lferrarotti74/github-workflows-templates/.github/workflows/sync-main-to-dev.yml@e66d6a30adb38b6b76b55ac9d0b8758f345ef3e4
    with:
      source_branch: ${{ inputs.source_branch }}
      target_branch: ${{ inputs.target_branch }}